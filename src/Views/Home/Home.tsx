import Landing from '../Landing/Landing';
import styles from './Home.module.scss';

const Home = () => {
    return (
        <div className={styles.home}>
            <Landing />
        </div>
    );
}

export default Home;
